/**
 * Infrastructure adapters layer (reserved).
 *
 * Planned responsibilities:
 * - Kafka producers/consumers for distributed ingestion pipelines
 * - persistence adapters for PostgreSQL / Elasticsearch
 * - outbound integrations with other platform services
 *
 * Keep domain and application core independent of these adapters.
 */
package com.obsyl.ingestion.infrastructure;
